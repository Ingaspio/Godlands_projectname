using System.Collections;
using System.Collections.Generic;
using Unity.IO.LowLevel.Unsafe;
using UnityEngine;

public static class DamageSystem
{
    //public static void RecieveDamage(CharacterStatsSO dealer, CharacterStatsSO reciever)
    //{
    //    dealer.damage -= reciever.currentHealth;
    //    if (reciever.currentHealth <= 0) { reciever.death = true; }
    //}
    //public static void DealDamage(CharacterStatsSO dealer, CharacterStatsSO reciever)
    //{
    //    dealer.damage -= reciever.currentHealth;
    //    if (reciever.currentHealth <= 0) { reciever.death = true; }
    //}
    //public static void Heal(CharacterStatsSO dealer, CharacterStatsSO reciever) 
    //{ 
    //    dealer.heal += reciever.currentHealth;
    //    reciever.currentHealth = reciever.currentHealth >= reciever.maxHealth ? reciever.currentHealth : reciever.maxHealth;
    //}
}